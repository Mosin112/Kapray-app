# Expo loads .env from this directory (apps/mobile/.env).
# EXPO_PUBLIC_* values are embedded in the client bundle — anon key only, never
# the service-role key.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
